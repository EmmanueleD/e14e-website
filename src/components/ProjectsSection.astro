---
import projects from "../data/projects.json";
---

<section id="projects" class="py-32 bg-black/20">
  <div class="max-w-7xl mx-auto px-6">
    <div class="flex flex-col md:flex-row justify-between items-end mb-16 gap-6">
      <div>
        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Progetti <span class="text-accent-orange">Selezionati</span>
        </h2>
        <p class="text-text-muted text-lg max-w-2xl">
          Una raccolta dei miei lavori più recenti, dove design e codice si incontrano.
        </p>
      </div>
      <a href="#" class="text-white hover:text-accent-orange transition-colors flex items-center gap-2 font-medium group">
        Vedi tutti i progetti
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {projects.map((project) => (
        <article class="group cursor-pointer">
          <div class={`aspect-[4/3] rounded-xl mb-6 overflow-hidden ${project.image} relative`}>
            <div class="absolute inset-0 bg-black/20 group-hover:bg-transparent transition-colors duration-500"></div>
            <!-- Overlay on hover -->
            <div class="absolute inset-0 bg-accent-orange/90 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
              <span class="text-white font-bold text-lg tracking-wider uppercase border border-white px-6 py-2 rounded-full">View Case Study</span>
            </div>
          </div>
          
          <div class="flex items-center gap-3 mb-3">
            <span class="text-accent-orange text-xs font-bold tracking-wider uppercase">{project.category}</span>
            <div class="h-px flex-1 bg-white/10"></div>
          </div>
          
          <h3 class="text-2xl font-bold text-white mb-3 group-hover:text-accent-orange transition-colors">{project.title}</h3>
          <p class="text-text-muted mb-4 line-clamp-2">{project.description}</p>
          
          <div class="flex flex-wrap gap-2">
            {project.tags.map((tag) => (
              <span class="text-xs text-white/60 bg-white/5 px-3 py-1 rounded-full">{tag}</span>
            ))}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
